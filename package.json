{
  "name": "erp-system",
  "version": "12.0.0",
  "description": "ERP System — Web Application",
  "private": true,
  "scripts": {
    "build": "cd frontend_src && npm install && npm run build",
    "dev": "cd frontend_src && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
