{
  "name": "erp-system",
  "version": "12.0.0",
  "description": "ERP System — Web Application",
  "private": true,
  "scripts": {
    "build": "cd frontend_src && npm install && npm run build",
    "build:manual": "cd docs/manual && python -m mkdocs build --strict -d ../../static/manual",
    "build:all": "npm run build && npm run build:manual && npm run build:mobile",
    "dev": "cd frontend_src && npm run dev",
    "build:mobile": "cd ../erp-mobile && npm run build:web && node -e \"const{cpSync,rmSync,mkdirSync}=require('fs');for(const d of ['../erp-system/mobile-web','../erp-system/static/m']){rmSync(d,{recursive:true,force:true});mkdirSync(d,{recursive:true});cpSync('web-build',d,{recursive:true})}console.log('mobile-web/ and static/m updated')\""
  },
  "//build-order": "The manual lands in static/manual/, and Vite empties static/ on every build — so build:manual must run AFTER build, never before. build:all does both in the right order; the Dockerfile keeps them in separate stages and copies the manual in last. Running build alone is fine: with no manual present the app 404s /manual/ and Settings simply omits the button.",
  "//build-manual-deps": "build:manual needs the pins in docs/manual/requirements.txt: pip install -r docs/manual/requirements.txt",
  "engines": {
    "node": ">=18"
  }
}
